<h1>たすく しょうさい</h1>
  <p class="list_header_title">タスク名：<%= @task.title %></p>
  <p class="list_header_title">タスク内容：<%= @task.body %></p>
  <%= link_to "タスクを完了しご褒美を受け取る",task_tasks_reward_path(@task.id) %>
  <p>投稿日時：<%= @task.created_at.strftime('%Y/%m/%d') %></p>

<div class="new-comment">
  <%= render 'public/task_comments/task_comments', task: @task, task_comment: @task_comment %>
</div>
<!--コメント一覧部分-->
<!--divタグのclassをid="comments_area"に変更-->
<div id="comments_area">
  <%= render 'public/task_comments/comment_form', task: @task, task_comment: @task_comment %>
</div>

  <div>
    <td id="fas fa-flag<%= @task.id %>">
   <%= render "public/favorites/favorite", task: @task %>
    </td>

</div>

