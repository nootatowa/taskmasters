<% task.cards.each do |card| %>
  <div class="card">
  <h3 class="card_title"><%= card.title %></h3>
  <% if card.memo.size > 0 %>
  <div class="card_detail is-exist"><i class="fas fa-bars"></i></div>
  <% end %>
  </div>
  <% end
  
<%= link_to list_card_path(list, card), class:"cardDetail_link" do %>
      <div class="card">
        <h3 class="card_title"><%= card.title %></h3>
        <% if card.memo.size > 0 %>
          <div class="card_detail is-exist"><i class="fas fa-bars"></i></div>
        <% end %>
      </div>
    <% end %>
  
  

  <div class="cardWrapper">
    <div class="addCard">
    <i class="far fa-plus-square"></i>
    <%= link_to "さらにカードを追加", new_task_card_path(task), class: "addCard_link" %>
  </div>
</div>

<!--差し込んだ記述-->
 <%= render '/tasks/card',task: @task %>
<%= link_to "しょうさい",task_card_path(task.id,card.id) %>
