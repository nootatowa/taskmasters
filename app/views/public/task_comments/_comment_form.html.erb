<%= form_with(model:[task, task_comment],url: task_comments_path, lacal: false) do |f| %>
  <!--エラーメッセージ-->
  <%= f.text_area :comment, rows:'5',placeholder: "コメントをここに" %>
  <%= f.submit "送信する" %>
<% end %>


